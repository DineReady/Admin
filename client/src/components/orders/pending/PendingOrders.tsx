import React from "react";
import { IPendingOrders } from "../../../types";

const PendingOrders = ({ orders }: IPendingOrders) => {
    return <div>PendingOrders</div>;
};

export default PendingOrders;
